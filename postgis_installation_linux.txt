http://www.gis-blog.com/how-to-install-postgis-2-3-on-ubuntu-16-04-lts/



sudo -u postgres createuser -P ipim
admin_ipim

sudo -u postgres createdb -O ipim ipim

psql -h localhost -U ipim ipim

sudo -u postgres psql -c "CREATE EXTENSION postgis; CREATE EXTENSION postgis_topology;" ipim






python packges:


sudo apt-get install python-pip python-dev build-essential 
sudo pip install --upgrade pip 
sudo pip install --upgrade virtualenv 

sudo pip install sqlalchemy

sudo pip install flask
sudo pip install flask_session psycopg2



# install tmux
 sudo apt-get install tmux

 # ru	n tmux
 tmux

 # it will open a terminal
 python server.py 0.0.0.0 8008

 # don't press ctrl-c, just close your window.
 
 
 130.211.230.203:8111


Gg ssh: on Mac
ssh-keygen -t rsa -f ~/.ssh/my-ssh-key -C zhangjj2016
Pass: zjj940611

ssh -i ~/.ssh/my-ssh-key zhangjj2016@130.211.230.203